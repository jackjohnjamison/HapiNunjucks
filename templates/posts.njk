{% extends "furniture.njk" %}

{% block main %} 
   <h1>Hello bitches</h1>

    <p>{{model.message}}</p>

    <section> 
        {% for post in model.data.posts %}
            <h3>{{post.title}}</h3>
            <p>{{post.body}}</p>
        {% endfor %}    
    </section>
 
{% endblock %}

