{% extends "furniture.njk" %}

{% block content %}

    <section> 
        {% for post in model.data.posts %}
            <h3>{{post.title}}</h3>
            <p>{{post.body}}</p>
        {% endfor %}    
    </section>
 
{% endblock %}

