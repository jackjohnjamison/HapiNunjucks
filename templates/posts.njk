{% extends "furniture.njk" %}

{% block main %} 
   <h1>Hello bitches</h1>

    <p>{{message}}</p>

    <section> 
        {% for post in model.posts %}
            <h3>{{post.title}}</h3>
            <p>{{post.body}}</p>
        {% endfor %}    
    </section>
 
{% endblock %}

{% block scripts %}
    {% if model.debug %}
        <script> const modelDebug = {{model.debug | safe}} </script>
    {% endif %}
{% endblock %}

